The Marketing Humans — Final Color Motion Build

This build replaces the previous blue/negative motion treatment with the supplied full-colour motion film.

Key fixes:
- Original colour video is preserved; no grayscale/invert filter.
- Motion section now uses the TMH website's soft-white background rather than a separate grey video card.
- Video edges are softly masked into the page so the animation reads as a native scene rather than an embedded rectangle.
- Apple-style scroll entrance, subtle depth movement, viewport-based playback and reduced-motion handling are included.
- Header logo now adapts between light and dark sections.
- Oversized dark-section typography has extra line-box room so descenders do not clip at the bottom.

Upload the contents of this folder to the website root / GitHub Pages deployment.
