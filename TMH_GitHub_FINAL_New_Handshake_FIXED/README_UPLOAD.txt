UPLOAD THIS VERSION TO GITHUB

This build removes the previous handshake loader and uses the supplied Handshake icon.lottie animation converted to Handshake-New.json.

IMPORTANT: Upload/replace ALL files in this folder together. All assets are intentionally at the repository root so GitHub's upload UI does not miss the assets folder.

Files: index.html, styles.css, script.js, Handshake-New.json, favicon.png, TMH-mark-white.png, and the five service PNG icons.

Do not keep the old Handshake.json or old loader files.
